import { Box, Typography } from '@mui/material';
import React from 'react';

export const AboutUsChip = (props) => {
    return (
        <Box sx={{width: '600px', height: '150px', backgroundColor: '#fff', borderRadius: '20px', m: 2}}>

        <Typography variant='h2'>Ethathu Pannuvom Inga</Typography>
        </Box>
    );
};
